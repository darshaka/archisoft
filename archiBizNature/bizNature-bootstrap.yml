spring:
  application:
    name: buzNatureService
  could:
    config:
      uri:  http://localhost:8888

management:
  security:
    enable: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

#logging:
#  file: elk-example.log
#  pattern:
#    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
#    console: "%d {yyyy-MM-dd HH:mm:ss} - %msg%n"